# SEO Optimization Plan — smphd.com + LinkedIn

Audit date: 2026-04-01

---

## Critical

| # | Change | File(s) | How To |
|---|---|---|---|
| 1 | Change `description` from `"personal description"` to keyword-rich site description | `_config.yml:15` | Replace `description: &description "personal description"` with something like `description: &description "Samuel Mathews, Ph.D. — Computational Scientist specializing in molecular dynamics (LAMMPS), DFT (VASP), and HPC. 11 publications, trilingual EN/FR/ES."`. This string is the site-wide fallback for `<meta name="description">` and `og:description` on every page. Keep it under 160 characters. |
| 2 | Change `url` from `http://localhost:4000` to `https://smphd.com` | `_config.yml:16` | Replace `url: http://localhost:4000` with `url: https://smphd.com`. This controls canonical URLs, OG URLs, the sitemap domain, and all JSON-LD structured data. Every SEO tag on the live site currently points to localhost. When developing locally, use `_config_docker.yml` or pass `--url http://localhost:4000` as a CLI flag instead of hardcoding localhost in the main config. |
| 3 | Add `description:` frontmatter to every page | All `_pages/*.md` | In the YAML frontmatter of each page, add a `description:` field with a unique 150-160 character summary. The `seo.html` template uses `page.description` as the first fallback for `<meta name="description">` and `og:description`. Example for `publications.md`: `description: "11 peer-reviewed publications on molecular dynamics, DFT, and gas hydrate systems by Samuel Mathews, Ph.D."`. Do this for: `about.md`, `experience.md`, `education.md`, `skills.md`, `awards.md`, `publications.md`, `talks.md`, `resume.md`, `sitemap.md`. |
| 4 | Set `og_image` to profile photo path | `_config.yml:139` | Replace blank `og_image:` with `og_image: "profile-shot.png"`. The `seo.html` template prepends `/images/` automatically, so this resolves to `/images/profile-shot.png`. This image appears in LinkedIn, Slack, and Twitter previews when someone shares any page on the site. |
| 5 | Add `excerpt:` to all pages so `og:description` emits | All `_pages/*.md` | The `seo.html` template (line 51) only emits `og:description` if `page.excerpt` exists. For static pages (non-posts), Jekyll doesn't auto-generate excerpts. Add `excerpt: "..."` to each page's frontmatter with the same text as `description:`. Alternatively, patch `seo.html` line 51 to also check `page.description` (change `{% if page.excerpt %}` to `{% if page.excerpt or page.description %}`). The patch approach is cleaner — one change fixes all pages. |
| 6 | Set `twitter.username` so Twitter Card block renders | `_config.yml:134` | Replace blank `username: &twitter` with your X/Twitter handle (e.g., `username: &twitter "smphd"`). If you don't have/want a Twitter account, an alternative is to bypass the `{% if site.twitter.username %}` conditional in `seo.html` by adding Twitter Card meta tags directly in `_includes/head/custom.html`: `<meta name="twitter:card" content="summary">`, `<meta name="twitter:title" content="{{ page.title }}">`, etc. |
| 7 | Populate `social.links` array | `_config.yml:145` | Replace the empty `links:` under `social:` with an array of profile URLs. This directly powers the JSON-LD `sameAs` property, which tells Google these profiles all belong to the same person: `links: ["https://www.linkedin.com/in/smphd", "https://orcid.org/0000-0003-0657-8623", "https://scholar.google.ca/citations?user=DrgCwWwAAAAJ&hl=en", "https://github.com/SamLMB"]` |
| 8 | Fix `/resume/` page: demote 6 of 7 H1s to H2s | `_pages/resume.md` | Currently Experience, Education, Technical Skills, Awards & Achievements, Publications, Presentations & Conferences are all `# H1`. Change them to `## H2`. Only "Resume" (from the `title:` frontmatter) should remain as H1. Search engines expect exactly one H1 per page. Find every line starting with `# ` (single hash) in the body and change to `## `. The sub-sections already using `##` become `###`, and `###` become `####`. |

---

## High

| # | Change | File(s) | How To |
|---|---|---|---|
| 9 | Change homepage `title:` from `"Home"` to name + role keywords | `_pages/about.md:3` | In frontmatter, change `title: "Home"` to `title: "Samuel Mathews, Ph.D. — Computational Scientist"`. The `title:` field sets both the `<title>` tag (as `Samuel Mathews, Ph.D. — Computational Scientist - Samuel Mathews`) and the H1 on the page. This is the single most important on-page SEO signal. |
| 10 | Homepage H1 updates automatically | `_pages/about.md:3` | No separate action — the H1 is generated from the `title:` frontmatter field changed in #9. Confirm after deploy that the rendered H1 matches the new title. |
| 11 | Fix heading hierarchy skip on homepage (H1 -> H4) | `_pages/about.md:10,14,18` | Change `#### English`, `#### Español`, `#### Français` to `## English`, `## Español`, `## Français`. Heading levels must not skip (H1 -> H4 is invalid). H2 is the correct level for subsections under the page H1. |
| 12 | Rewrite `skills.md` with actual tool names and missing categories | `_pages/skills.md` | Replace generic descriptions with searchable names. "Large Atomic/Molecular Modeling Software Package" -> "LAMMPS". "Vienna Ab Initio Simulation Package" -> "VASP". Add missing tools: OVITO, VMD, Moltemplate, Packmol, PyTorch, COMSOL, Simulink, LabVIEW, EasyBuild, CMake. Add missing categories: **Force Fields** (TIP4P/Ice, OPLS-AA, EPM2, TraPPE), **Numerical Methods** (FEM, Monte Carlo, Newton-Raphson, spectral methods, finite differences), **Visualization** (OVITO, VMD), **ML/AI** (PyTorch, GNNs, scikit-learn with specific algorithms). Recruiters search for tool names, not full formal names. |
| 13 | Fix typo: "Artifical Intelligence" -> "Artificial Intelligence" | `_pages/skills.md:22` | Find `## Artifical Intelligence` and replace with `## Artificial Intelligence`. |
| 14 | Fix "Material Modeling" -> "Materials Modeling" | `_config.yml:29` | In the `bio:` field, change `Material Modeling` to `Materials Modeling`. The industry-standard term is plural. This text appears in the sidebar on every page. |
| 15 | Remove or exclude dev/placeholder pages from build | `_pages/markdown.md`, `_pages/non-menu-page.md`, `_pages/page-archive.html`, `_pages/year-archive.html`, `_pages/tag-archive.html`, `_pages/terms.md` | Option A (preferred): add `published: false` to the frontmatter of each junk page — Jekyll will not build or include them in the sitemap. Option B: move them to a `_drafts/` folder. Option C: add them to the `exclude:` list in `_config.yml`. Pages like `/markdown/` expose internal Jekyll documentation ("Locations of key files/directories") to Google. `/non-menu-page/` is a literal test page saying "This is a page not in the menu." |
| 16 | Set up Google Search Console | `_config.yml:126` | Go to [search.google.com/search-console](https://search.google.com/search-console), add `smphd.com` as a property. Choose "URL prefix" method. Copy the verification meta tag content string. Paste it into `google_site_verification:` in `_config.yml`. Deploy. Verify in Search Console. Then submit `https://smphd.com/sitemap.xml` under Sitemaps. |
| 17 | Set up Bing Webmaster Tools | `_config.yml:127` | Go to [bing.com/webmasters](https://www.bing.com/webmasters), add site, get verification string, paste into `bing_site_verification:` in `_config.yml`. Deploy and verify. Bing also powers DuckDuckGo and Yahoo search. |
| 18 | Enable Google Analytics 4 | `_config.yml:150-152` | Create a GA4 property at [analytics.google.com](https://analytics.google.com). Get the Measurement ID (format: `G-XXXXXXXXXX`). Set `provider: "google-analytics-4"` and `tracking_id: "G-XXXXXXXXXX"` in `_config.yml`. The template's `analytics.html` include handles the script injection. |
| 19 | Set `social.type` explicitly to `"Person"` | `_config.yml:143` | Change `type:` (blank) to `type: "Person"`. While the template defaults to Person when blank, being explicit prevents ambiguity and makes the JSON-LD clearer for crawlers. |
| 20 | Set `social.name` | `_config.yml:144` | Change `name:` (blank) to `name: "Samuel Mathews"`. This populates the `name` field in the JSON-LD Person schema. When blank, it falls back to `site.name`, which works — but explicit is better for structured data. |
| 21 | Change JSON-LD `@context` from `http://` to `https://` | `_includes/seo.html:107,118` | Find both instances of `"@context": "http://schema.org"` and change to `"@context": "https://schema.org"`. Schema.org officially supports HTTPS and Google recommends it. There are exactly two instances — one in the Organization block (line 107) and one in the Person block (line 118). |
| 22 | Add unique `description:` frontmatter to each page | All `_pages/*.md` | This is the per-page implementation of #3. Suggested descriptions (each under 160 chars): **about.md**: `"Samuel Mathews, Ph.D. — Trilingual computational scientist (EN/FR/ES) specializing in molecular dynamics, DFT, and HPC. 11 publications, 29 presentations."` **experience.md**: `"Professional experience: graduate researcher, HPC systems administrator, and lab manager at McGill University. Molecular dynamics, cluster management, procurement."` **education.md**: `"Education: Ph.D. and M.Eng. in Chemical Engineering from McGill University. Coursework in computational methods, thermodynamics, and fluid mechanics."` **skills.md**: `"Technical skills: LAMMPS, VASP, Python, Docker, HPC, machine learning, data science, NixOS. Trilingual: English, French, Spanish."` **awards.md**: `"Awards: FRQNT Doctoral Scholarship, MEDA, CACSE Travel Award, Presentation Excellence Award, Best Use of Language (French)."` **publications.md**: `"11 peer-reviewed publications on molecular dynamics, DFT, and gas hydrate systems. 7 first-author. DOI links to all papers."` **talks.md**: `"29 presentations at international conferences including MRS, APS, WCCM, BIRS, and CSChE. Talks in English, French, and Spanish."` **resume.md**: `"Resume of Samuel Mathews, Ph.D. — Computational scientist, HPC administrator, trilingual. Download PDF or view inline."` |
| 23 | Resolve LinkedIn URL inconsistency | `cv/resume_SE.tex`, `cv/resume_ECV.tex`, `cv/resume_CON.tex`, `_config.yml:64` | The website config and `resume_AS.tex` use `linkedin.com/in/smphd`. The other resume .tex files use `linkedin.com/in/smlmathews`. Decide which is the canonical URL. If the actual LinkedIn custom URL is `/in/smphd`, update the three .tex files. If it's `/in/smlmathews`, update `_config.yml` and `resume_AS.tex`. Search each .tex file for `linkedin` and replace. Consistent URL across all materials strengthens cross-platform SEO signals. |

---

## Medium

| # | Change | File(s) | How To |
|---|---|---|---|
| 24 | Create `robots.txt` with disallow rules | `robots.txt` (new file, repo root) | No `robots.txt` exists. Create one in the repo root with: `User-agent: *`, `Disallow: /markdown/`, `Disallow: /non-menu-page/`, `Disallow: /markdown_generator/`, `Disallow: /page-archive/`, `Disallow: /year-archive/`, `Disallow: /tags/`, `Disallow: /terms/`, blank line, `Sitemap: https://smphd.com/sitemap.xml`. This tells crawlers not to index junk/thin pages. Combined with #15 (removing from build), this is defense in depth. |
| 25 | Add `lastmod` dates to sitemap entries | All `_pages/*.md` | The `jekyll-sitemap` plugin reads `last_modified_at:` from frontmatter. Add `last_modified_at: 2026-04-01` (or actual last-edit date) to every page's frontmatter. This populates `<lastmod>` in `sitemap.xml`, telling search engines when content was last updated so they can prioritize fresh crawls. Update these dates whenever you edit a page. |
| 26 | Enforce HTTPS redirect | GitHub repo settings | Go to the `samlmb.github.io` repo on GitHub -> Settings -> Pages. Check "Enforce HTTPS". This makes `http://smphd.com` 301-redirect to `https://smphd.com` instead of serving duplicate content. Currently `http://smphd.com` returns 200 OK without redirect, which means Google may index both versions. |
| 27 | Add `lang` attributes to FR/ES content on homepage | `_pages/about.md` | Wrap the Spanish paragraph in `<div lang="es">...</div>` and the French paragraph in `<div lang="fr">...</div>`. This tells Google these sections are in different languages, preventing it from treating them as garbled English or duplicate content. Keep the English section unwrapped (inherits `lang="en"` from the page `<html>` tag). |
| 28 | Convert profile image from PNG to WebP | `images/profile-shot.png` | Use `cwebp images/profile-shot.png -o images/profile-shot.webp` (install via `apt install webp`). Then update `_config.yml:25` to `avatar: "profile-shot.webp"`. Expected size reduction: 152KB -> ~30-50KB. Also check `og_image` (set in #4) points to the new filename. Keep the PNG as fallback if needed for older browsers, but WebP has 97%+ browser support in 2026. |
| 29 | Add default `og:type: website` for non-dated pages | `_includes/seo.html` | Currently `og:type` is only set for pages with a `date` field (line 92-95, sets it to `article`). Most pages have no date and get no `og:type`. After line 95, add: `{% unless page.date %}<meta property="og:type" content="website">{% endunless %}`. This ensures every page declares a type for social media parsers. |
| 30 | Update `employer` field | `_config.yml:31` | Change `employer: "McGill University"` to reflect current status. Options: remove it entirely, change to `"Open to opportunities"`, or leave as McGill until graduation. This appears in the sidebar author profile. After April 2026 graduation it should definitely change. |
| 31 | Remove "financial markets" from bio and update target sectors | `_pages/about.md` | In all three language versions, remove "financial markets" / "mercados financieros" / "marchés financiers". Replace with target sectors: simulation software, pharmaceutical, and energy. Also update "8 years of experience" if the count has changed. The bio should reflect current job search targets, not removed sectors. |
| 32 | Fix resume PDF link from HTTP to HTTPS | `_pages/resume.md:10` | Change `[here](http://smphd.com/files/resume.pdf)` to `[here](https://smphd.com/files/resume.pdf)`. Or better: use a relative path `[here](/files/resume.pdf)` which always resolves to the current protocol. Mixed HTTP/HTTPS content is a minor negative signal. |
| 33 | Fix footer Jekyll link from HTTP to HTTPS | Layout template (likely `_includes/footer.html` or `_layouts/default.html`) | Search for `http://jekyllrb.com` and replace with `https://jekyllrb.com`. Run: `grep -r "http://jekyllrb" samlmb.github.io/` to find the exact file. Minor mixed-content signal but easy fix. |
| 34 | Add ScholarlyArticle JSON-LD markup for publications | `_pages/publications.md` or `_includes/head/custom.html` | Add a JSON-LD `<script>` block to the publications page with `@type: ScholarlyArticle` for each publication. Minimal per-article schema: `{"@type": "ScholarlyArticle", "headline": "Paper Title", "author": [{"@type": "Person", "name": "Mathews, S."}], "datePublished": "2026", "url": "https://doi.org/10.1021/..."}`. This helps Google Scholar and AI search engines parse individual papers. Can be done manually in the page body or templated if publications move to a Jekyll collection with individual files. |

---

## LinkedIn

| # | Change | File(s) | How To |
|---|---|---|---|
| 35 | Set "Open to Work" (Recruiters Only) | LinkedIn settings | LinkedIn -> Settings -> Job seeking preferences -> Job preferences -> toggle "Open to work". Set visibility to "Recruiters only". Add all 5 target titles: Applications Scientist, Solutions Engineer, Computational Scientist, Technical Consultant, Field Applications Engineer. Add target locations: United States, Canada (excluding Quebec unless $200K+ CAD). This is the single highest-impact LinkedIn signal — recruiters get a green "Open to Work" spotlight filter. |
| 36 | Rewrite headline | LinkedIn profile | Go to profile -> click pencil icon on intro card -> edit Headline. New headline: `Computational Scientist | Molecular Dynamics & DFT | PhD Chemical Engineering | Trilingual EN/FR/ES`. 220 char limit, but only ~60 visible on mobile — front-load the target job title. LinkedIn Recruiter Boolean searches match against headline text, so include the exact titles recruiters search for. |
| 37 | Rewrite About section | LinkedIn profile | Go to profile -> About -> edit. 2,600 char limit. **First 300 characters** (visible before "See more") must hook: `"Trilingual computational scientist (EN/FR/ES) with 11 peer-reviewed publications, 29 international presentations, and 8 years of experience in molecular dynamics, DFT, and HPC. PhD in Chemical Engineering from McGill University (2026)."` **Body**: 2-3 short paragraphs covering research impact, technical depth, and what you're looking for. **End with keyword block**: `Core Competencies: Molecular Dynamics, DFT, LAMMPS, VASP, Python, HPC, Machine Learning, Docker, Data Science, Technical Consulting, Solutions Architecture, Simulation Software, Pharma, Energy`. Recruiters' keyword filters scan the entire profile. |
| 38 | Fill all 50 skill slots | LinkedIn profile | Go to profile -> Skills -> Add skill. **Top 3** (get most algorithmic weight — pin these): Computational Science, Molecular Modeling, Python. **Next 47**: LAMMPS, VASP, Molecular Dynamics, Density Functional Theory, High Performance Computing, Machine Learning, Data Science, Docker, Scientific Computing, Materials Science, Simulation, NumPy, pandas, scikit-learn, SciPy, Matplotlib, Bash, SQL, LaTeX, Linux, NixOS, Slurm, Git, Technical Presentations, Grant Writing, HPC Administration, Cluster Management, Data Analysis, Statistical Analysis, Parallel Computing, COMSOL, MATLAB, Force Field Development, Phonon Calculations, Chemical Engineering, Process Simulation, Research, Scientific Writing, Mentoring, Teaching, Project Management, Technical Consulting, Cross-Functional Communication, Troubleshooting, Procurement, Vendor Management, InfiniBand. Ask colleagues to endorse your top skills — endorsements boost recruiter search ranking. |
| 39 | Create FR and ES profile versions | LinkedIn settings | Desktop: profile -> top right "Add profile in another language". Create a French version and a Spanish version. Each gets its own URL (e.g., `linkedin.com/in/smphd?locale=fr_FR`) and is indexed separately by Google. LinkedIn auto-shows the version matching the viewer's interface language. Translate: headline, about, experience descriptions, education. Skills and endorsements only appear in the primary language. Do NOT create separate accounts (violates TOS). |
| 40 | Confirm custom URL is `/in/smphd` | LinkedIn settings + `cv/*.tex` | LinkedIn -> Settings -> Edit public profile & URL -> edit custom URL. Verify it is `smphd` (matching `_config.yml`). Then update any .tex files using `smlmathews` to `smphd`. Run `grep -r "smlmathews" cv/` to find all instances. A consistent URL across all materials strengthens the cross-platform entity graph for search engines. |
| 41 | Add Featured section | LinkedIn profile | Go to profile -> Add section -> Featured. Upload: (1) the industry-targeted resume PDF (AS or SE variant, not ECV), (2) a slide deck from a strong technical presentation (e.g., MRS or WCCM talk — proves presentation skills critical for AS/SE roles), (3) optionally link to smphd.com or a GitHub repo. Featured items appear prominently and increase profile dwell time. |

---

## Cross-Platform

| # | Change | File(s) | How To |
|---|---|---|---|
| 42 | Populate `social.links` array in `_config.yml` | `_config.yml:145` | Same as #7. Ensure the array includes exactly: `["https://www.linkedin.com/in/smphd", "https://orcid.org/0000-0003-0657-8623", "https://scholar.google.ca/citations?user=DrgCwWwAAAAJ&hl=en", "https://github.com/SamLMB"]`. This creates a circular verification loop: your site claims these profiles via `sameAs`, and those profiles link back to your site (see #44-46). Google uses this bidirectional linking to build an Entity Graph and may trigger a Knowledge Panel. |
| 43 | Add `@id` to JSON-LD Person schema using ORCID | `_includes/seo.html:116-125` | In the Person JSON-LD block, add `"@id": "https://orcid.org/0000-0003-0657-8623"` as a field. ORCID is a globally unique persistent identifier — it tells search engines unambiguously "this is the same person across all platforms." The modified block should look like: `{"@context": "https://schema.org", "@type": "Person", "@id": "https://orcid.org/0000-0003-0657-8623", "name": "Samuel Mathews", "url": ..., "sameAs": ...}`. Also consider adding `"jobTitle": "Computational Scientist"`, `"alumniOf": {"@type": "CollegeOrUniversity", "name": "McGill University"}`, and `"knowsLanguage": ["English", "French", "Spanish"]` to enrich the structured data. |
| 44 | Verify Google Scholar homepage points to smphd.com | Google Scholar settings | Go to [scholar.google.com](https://scholar.google.com) -> your profile -> edit (pencil icon) -> Homepage field. Set to `https://smphd.com`. This creates a backlink from a high-authority Google property to your site and closes the cross-platform verification loop. Also verify: profile is Public (not private), photo is uploaded, "Areas of Interest" use industry keywords (e.g., "Molecular Dynamics, DFT, Computational Materials Science, HPC, Machine Learning" rather than purely academic terms). |
| 45 | Verify ORCID links to smphd.com and LinkedIn | ORCID settings | Go to [orcid.org](https://orcid.org) -> sign in -> edit profile. Under "Websites & Social Links", add: `https://smphd.com` (label: "Personal Website"), `https://www.linkedin.com/in/smphd` (label: "LinkedIn"). Set all Works to **Public** visibility (many are accidentally set to "Trusted Organizations" and invisible to external search). Use "Featured Works" to pin 3-5 key publications. Connect Scopus Author ID and Crossref for automatic work import. |
| 46 | Verify GitHub profile links to smphd.com | GitHub profile settings | Go to [github.com/settings/profile](https://github.com/settings/profile). Set "Website" to `https://smphd.com`. Set "Bio" to something like "PhD Chemical Engineering | Molecular Dynamics & DFT | Trilingual EN/FR/ES". This creates another backlink and closes the verification loop. Also set the `samlmb.github.io` repo description and topics (add: `molecular-dynamics`, `computational-science`, `dft`, `hpc`, `lammps`, `vasp`). |

---

## Content

| # | Change | File(s) | How To |
|---|---|---|---|
| 47 | Rewrite `about.md` bio in all 3 languages | `_pages/about.md` | Remove "financial markets" / "mercados financieros" / "marchés financiers" from all three versions. Replace with current target sectors. Update "8 years of experience" to accurate count. Rewrite to target industry keywords: lead with "computational scientist" framing, mention LAMMPS/VASP/HPC by name, include "11 publications" and "trilingual" as differentiators. Keep each version to 2-3 sentences max. The homepage bio is the most-read content on the site — every word should serve SEO or conversion. |
| 48 | Full rewrite of `skills.md` | `_pages/skills.md` | Current page uses formal/generic names and is missing many tools. Rewrite with these categories and actual tool names: **Languages**: English (Native), French (Native), Spanish (Native). **Programming**: Python, MATLAB, Bash, SQL, LaTeX. **Simulation Software**: LAMMPS, VASP, Phonopy, MDAnalysis, OVITO, VMD, Moltemplate, Packmol, COMSOL, Simulink, LabVIEW. **Force Fields**: TIP4P/Ice, OPLS-AA, EPM2, TraPPE, Zhang CO2 Potential. **Data Science & ML**: NumPy, pandas, SciPy, scikit-learn, Matplotlib, PyTorch, GNNs (in progress), MLIPs (in progress), classifiers, regression, SVM, text mining. **Numerical Methods**: FEM/Galerkin, finite differences, spectral methods, Monte Carlo integration, Newton-Raphson, bifurcation theory, arc-length continuation. **HPC**: Slurm, CVMFS, InfiniBand, MPI (OpenMPI, Intel MPI), EasyBuild, CMake, GPU programming. **Infrastructure**: NixOS, Debian, Docker/Docker Compose, LDAP, DHCP/DNS, Nginx Proxy Manager, Gitea, 3-2-1 backup. **Visualization**: OVITO, VMD, Matplotlib. **Version Control**: Git, Gitea (self-hosted). **Networking**: HTTPS/SSL, traffic routing, DHCP, DNS, custom domains. |
| 49 | Add unique `description:` frontmatter to every page | All `_pages/*.md` | Same as #22. This is listed twice because it spans both "High" (as a structural SEO fix) and "Content" (as a writing task). See #22 for the suggested per-page descriptions. Each description must be unique, keyword-rich, and under 160 characters. These appear in Google search results as the snippet below the page title. |
